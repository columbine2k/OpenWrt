# Docker 生态暂时从 ImmortalWrt 线整体移除（2026-08-21）
#
# 原因：dockerd 的 iptables/nftables 操作与 fw4/nftables 存在兼容性问题。
# 该文件在 workflow 中于 shared/extra.config 之后追加，用于把
# extra.config 里的 docker 相关条目强制关掉（kconfig 后出现者生效），
# 因此 LEDE 线不受影响。
#
# 将来恢复时：删除本文件、去掉两个 workflow 里的追加行，
# 并在 immortalwrt/configs/*.config 中重新启用相关包即可。
#
# CONFIG_PACKAGE_luci-app-dockerman is not set
# CONFIG_PACKAGE_docker-compose is not set
